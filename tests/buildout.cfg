[buildout]
parts =
    django
versions = versions
eggs = 
	django-icetea
    django-extensions
develop = ..	

[versions]
django = 1.3.1

[django]
recipe = djangorecipe
control-script = manage
project = project
eggs = ${buildout:eggs}
settings = settings
test = app
testrunner = test


