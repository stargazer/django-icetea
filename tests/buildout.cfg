[buildout]
parts = django                                           
eggs = 
	django-icetea 
    django-extensions

[django]
recipe = djangorecipe
control-script = manage
project = project
eggs = ${buildout:eggs}
settings = settings
test = app
testrunner = test

[versions]
django = 1.3

